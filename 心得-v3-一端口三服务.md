# 心得：一个端口三服务 + 统一密码（v3 改造记录）

> 2026-08-14 · chromium-QwenPaw v3
> 核心一句话：**部署只需填一个公网主端口 `-q`，install.sh 自动推导出 VNC / SSH / QwenPaw 面板三条 frp 隧道；密码也统一成一个 `-P`，VNC 和 SSH root 共用。**

---

## 一、为什么做这次改造

v2 的部署参数虽然无交互，但有点啰嗦：

```bash
bash install.sh -s 你的IP -t 你的TOKEN -q 10000 -v 20000 -S 20022 -P browser123
```

痛点：

1. **要填 4~5 个端口/密码参数**，每个都要自己动脑想"这个端口用来干嘛、会不会冲突"，心智负担大。
2. **VNC 密码和 SSH 密码是两套**。VNC 用 `-P browser123`，SSH root 密码却是另一串（recover-frp.sh 里硬编码的 `dajpaq-...`），用户记不住。
3. **端口分配没有规律**，部署完还要回头查"哪个端口是 VNC 哪个是 SSH"。

用户的需求一句话点醒：**"要不 QWENPAW_REMOTE_PORT 做"** —— 干脆拿一个主端口，剩下的都自动推出来。

## 二、设计决策

### 1. 一个主端口 N → 三条隧道

```
-q N
├── VNC/html     = N      (主端口, 用户最常访问)
├── SSH          = N-1    (少一位, 方便记)
└── QwenPaw面板  = N-2    (再少一位)
```

选这个方向的原因：
- **VNC 最常用**，给主端口 N，用户记忆负担最小（`http://IP:N` 就是桌面）。
- SSH / 面板依次减一，**规则简单好推导**，部署完不用查文档。
- 主端口本身由用户自选（不冲突即可），所以 N-1 / N-2 冲突概率极低。

### 2. 保留覆盖能力（向后兼容）

自动推导不等于死板。`-v / -S / -Q` 仍然可用，显式设置了就用显式的：

```bash
bash install.sh -s IP -t TOKEN -q 10000 -v 20000 -S 20022   # 手动覆盖 VNC/SSH
bash install.sh -s IP -t TOKEN -q 10000 -S 0                # 显式传 0 = 禁用 SSH 隧道
```

用 `VNC_SET / SSH_SET / QP_SET` 三个标志位区分"没传"和"传了 0"：
- 没传 → 自动推导
- 传了端口 → 用显式值
- 传了 0 → 禁用该隧道（不留隧道）

### 3. 统一密码：一个 `-P` 管 VNC + SSH

```bash
PASSWORD="${PASSWORD:-browser123}"          # 统一密码
VNC_PASS="${VNC_PASS:-$PASSWORD}"           # VNC 用统一密码
SSH_PASSWORD="${SSH_PASSWORD:-$PASSWORD}"   # SSH root 也用统一密码
```

- `-P <密码>` 同时设置 VNC 密码和 SSH root 密码，**用户只需记一个密码**。
- 想单独分开？`VNC_PASS` / `SSH_PASSWORD` 环境变量仍可单独覆盖。
- install.sh 顺带把 sshd 的配置也做了：装 openssh-server（容器重建后常缺）→ chpasswd 设 root 密码 → 允许 root 密码登录 → 启动 sshd → 验证本地 22 端口可连。**SSH 隧道不再是"只转发不服务"**，装完就能 `ssh -p N-1 root@IP`。

## 三、核心改动

### 1. install.sh：参数语义升级

```bash
# 旧: -q = QwenPaw 面板端口, -v/-S 要单独填
-q|--qwenpaw)  QWENPAW_REMOTE_PORT="$2"; shift 2 ;;
-v|--vnc)      FRP_VNC_REMOTE_PORT="$2"; shift 2 ;;
-S|--ssh)      FRP_SSH_REMOTE_PORT="$2"; shift 2 ;;

# 新: -q = 公网主端口, 自动推导; -P = 统一密码; -Q/-v/-S 变覆盖项
-q|--port)     QWENPAW_REMOTE_PORT="$2"; shift 2 ;;
-P|--password) PASSWORD="$2"; VNC_PASS="$2"; shift 2 ;;
-Q|--qwenpaw)  QWENPAW_PANEL_PORT="$2"; QP_SET=1; shift 2 ;;
-v|--vnc)      FRP_VNC_REMOTE_PORT="$2"; VNC_SET=1; shift 2 ;;
-S|--ssh)      FRP_SSH_REMOTE_PORT="$2"; SSH_SET=1; shift 2 ;;
```

端口推导段（放在参数解析后、frpc.toml 生成前）：

```bash
[ "${VNC_SET:-}" = "1" ] || FRP_VNC_REMOTE_PORT="$((QWENPAW_REMOTE_PORT))"
[ "${SSH_SET:-}" = "1" ] || FRP_SSH_REMOTE_PORT="$((QWENPAW_REMOTE_PORT - 1))"
[ "${QP_SET:-}"  = "1" ] || QWENPAW_PANEL_PORT="$((QWENPAW_REMOTE_PORT - 2))"
# 显式传 0 禁用隧道
[ "$FRP_VNC_REMOTE_PORT" != "0" ] || FRP_VNC_REMOTE_PORT=""
[ "$FRP_SSH_REMOTE_PORT" != "0" ] || FRP_SSH_REMOTE_PORT=""
[ "$QWENPAW_PANEL_PORT"  != "0" ] || QWENPAW_PANEL_PORT=""
```

新增 SSH 服务段（frpc.toml 生成前）：装/启 sshd + chpasswd + 允许 root 登录 + 验证 22 端口。

### 2. recover-frp.sh：与 install.sh 对齐

容器重建后一键恢复，也走同一套"主端口推导 + 密码复用"：

```bash
REMOTE_PORT="${REMOTE_PORT:-30208}"                          # 主端口
SSH_REMOTE_PORT="${SSH_REMOTE_PORT:-$((REMOTE_PORT - 1))}"   # SSH = 主-1
VNC_REMOTE_PORT="${VNC_REMOTE_PORT:-$REMOTE_PORT}"           # VNC = 主
VNC_PASS="${VNC_PASS:-${PASSWORD:-browser123}}"              # VNC 密码
ROOT_PASSWORD="${SSH_PASSWORD:-$VNC_PASS}"                   # SSH 复用 VNC 密码
```

顺手修了个老 bug：chpasswd 判断 `if [ -z "$(echo ... | chpasswd 2>&1)" ]`（成功无输出 → 空 → 真，但失败也未必有输出）改成 `if printf 'root:%s\n' "$PASS" | chpasswd; then`。

### 3. README 全面同步

- 快速开始第 2 步：新模板只填 3 个必填 + 1 个可选密码
- 参数表：`-q` 改为主端口语义、新增 `-P` / `-Q`、`-v` / `-S` 标为"覆盖项"
- 访问说明：`面板=主端口-2, noVNC=主端口, SSH=主端口-1, 密码=PASSWORD`
- 恢复脚本表：`REMOTE_PORT` 主端口 + 密码变量
- 安全提示：noVNC 从"默认无密码"改为"默认 browser123，建议 -P 改强密码"

## 四、验证

```
-q 10000            → VNC=10000 SSH=9999 面板=9998   ✅
-q 10000 -v 20000   → VNC=20000 SSH=9999 面板=9998   ✅
-q 10000 -Q 9990    → VNC=10000 SSH=9999 面板=9990   ✅
-q 10000 -S 0       → VNC=10000 SSH=禁用 面板=9998   ✅
```

线上机器直接对齐：主端口 30208 → VNC=30208 / SSH=30207（与现状一致，无缝）。

## 五、收益

1. **部署一条命令**：`bash install.sh -s IP -t TOKEN -q 10000 -P 密码` 搞定全部。
2. **密码只记一个**：VNC 和 SSH root 同一个，不用再翻部署日志找 SSH 密码。
3. **端口规则一目了然**：`http://IP:N` 桌面、`ssh -p N-1`、`http://IP:N-2` 面板。
4. **恢复同样简单**：容器重建后 `REMOTE_PORT=10000 bash recover-frp.sh` 原样还原。

---

## 六、踩坑记录

1. **端口"留空"和"显式 0"语义要分清**。一开始想用 `[ -n "$VAR" ]` 判断"用户没传"，但自动推导后变量恒非空，没法区分"用户没传"和"用户传了同值"。最后用独立标志位 `VNC_SET/SSH_SET/QP_SET` 解决。
2. **chpasswd 判断陷阱**：`echo "root:pw" | chpasswd` 成功时无输出、失败时通常也无输出（错误走 stderr），`$( )` 截获后 `-z` 恒真，永远报成功。必须用 `if ... | chpasswd; then` 直接判断退出码。
3. **recover-frp.sh 的硬编码密码要删干净**：`ROOT_PASSWORD="dajpaq-..."` 是部署时留在脚本里的明文密码，改成变量后从默认值里彻底移除，避免再泄露。
