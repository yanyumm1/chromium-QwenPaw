# 心得合集：chromium-QwenPaw 浏览器/VNC 架构演进全记录

> 2026-08-18 合并整理 · 涵盖 v2 → v7 全部改造 + 源码考古
> 核心一句话：**从“两个浏览器各干各的”到“人机同屏 + 动态分辨率 + 控制栏收起 + 移动端全适配”，一路升级 VNC 桌面架构，并拆解了 Pterodactyl-Browser / Sap-FireFox / jlesage 三个参考项目的方案。**

---
## 目录

- [v2 有头 CDP：让人机同屏](#v2-有头-cdp让人机同屏)
- [v3 一端口三服务：部署一条命令](#v3-一端口三服务部署一条命令)
- [v4 jlesage 方案学习：Xvnc+nginx vs 我们](#v4-jlesage-方案学习)
- [v5 Xvnc 动态分辨率 + 控制栏收起](#v5-xvnc-动态分辨率--控制栏收起)
- [v6 白屏排查与孤儿进程](#v6-白屏排查与孤儿进程)
- [v7 移动端适配与部署简化](#v7-移动端适配与部署简化)
- [考古：chrome.sh 左侧菜单栏 & Sap-FireFox 固定画面](#考古chrome.sh-左侧菜单栏--sap-firefox-固定画面)
- [架构演进总览](#架构演进总览)

---
## v2 有头 CDP：让人机同屏

> 2026-08-14 · chromium-QwenPaw v2
> 核心：把 AI 自动化用的 Chromium（CDP 9222）从无头改成有头，直接显示在 VNC 桌面上，AI 操作浏览器时用户实时可见。

### 为什么做

v1 架构里有两个浏览器各干各的：

```
VNC 桌面 (Xvfb :1)
  ├── chromium-gui   ← 用户看到/操作的全屏浏览器 (Bing 首页)
  └── chromium-cdp   ← AI 自动化用的 (--headless=new, 9222 端口) ← 用户看不见
```

痛点：用户不知道 AI 在浏览器里干什么；两个浏览器 profile 互不相通；用户明确要"打开 vnc 应该就是 chromium-cdp 有头界面"——**人机同屏**。

### 核心改动

1. **chromium-cdp 去 `--headless=new`，加 `DISPLAY=:1`**：CDP 不受影响（`--remote-debugging-port=9222` 有头照样工作），去掉 `--kiosk`（会藏浏览器原生 UI），启动页改为 Tampermonkey 商店页。
2. **install.sh 加 `CDP_HEADED` / `CDP_START_URL` 开关**：有头=1（默认，VNC 可见），无头=0（省内存）。有头时 `chromium-gui` autostart 自动 false（避免两个浏览器抢桌面）。
3. **README 更新架构说明**：两个浏览器 → 有头合一模式。

### 踩坑

| 坑 | 解决 |
|----|------|
| `--kiosk` 藏了浏览器 UI | 去掉 kiosk，用正常窗口模式 |
| 两个 chromium 抢桌面 | 有头模式时 chromium-gui autostart=false |
| supervisor template 不同步 | conf + template 两处都改（项目铁律） |

---

## v3 一端口三服务：部署一条命令

> 2026-08-14 · chromium-QwenPaw v3
> 核心：部署只需填一个公网主端口 `-q`，install.sh 自动推导出 VNC / SSH / QwenPaw 面板三条 frp 隧道；密码也统一成一个 `-P`，VNC 和 SSH root 共用。

### 设计决策

**一个主端口 N → 三条隧道**：

```
-q N
├── VNC/html     = N      (主端口, 用户最常访问)
├── SSH          = N-1    (少一位, 方便记)
└── QwenPaw面板  = N-2    (再少一位)
```

**保留覆盖能力**：`-v / -S / -Q` 仍可用，显式设置就用显式的；传 0 = 禁用隧道。用 `VNC_SET / SSH_SET / QP_SET` 标志位区分"没传"和"传了 0"。

**统一密码**：`-P <密码>` 同时设置 VNC 密码和 SSH root 密码。install.sh 顺带装 openssh-server → chpasswd 设 root 密码 → 允许 root 登录 → 启动 sshd → 验证 22 端口。

### 验证

```
-q 10000            → VNC=10000 SSH=9999 面板=9998   ✅
-q 10000 -v 20000   → VNC=20000 SSH=9999 面板=9998   ✅
-q 10000 -S 0       → VNC=10000 SSH=禁用 面板=9998   ✅
```

### 踩坑

1. **端口"留空"和"显式 0"语义要分清**：自动推导后变量恒非空，用独立标志位解决。
2. **chpasswd 判断陷阱**：`$( )` 截获后 `-z` 恒真，必须用 `if ... | chpasswd; then` 判断退出码。
3. **recover-frp.sh 硬编码密码要删干净**。

---

## v4 jlesage 方案学习

> 2026-08-14 · chromium-QwenPaw v4
> 核心：拆解 jlesage/docker-chromium 源码，它是"Xvnc + nginx + noVNC"一体架构，跟我们的"Xvfb + x11vnc + websockify"是两条不同路线。

### 架构对比

```
jlesage 方案:
浏览器 ──▶ :5800 nginx ──websockify_pass──▶ unix:/tmp/vnc.sock ──▶ Xvnc (:0)
                 │                                                       │
                 └─ 定制 noVNC 前端(控制面板/剪贴板/缩放/文件管理/终端)     └─ TigerVNC 一体: X server + VNC server

我们的方案:
浏览器 ──▶ :8080 websockify(Python) ──▶ :5900 x11vnc ──▶ :1 Xvfb ── xfce4 桌面
```

| 维度 | jlesage (Xvnc+nginx) | 我们 (Xvfb+x11vnc+websockify) |
|---|---|---|
| X server + VNC | Xvnc 一体，省进程 | Xvfb + x11vnc 两个进程 |
| WebSocket 桥 | nginx 内置模块，零进程 | 自带 Python websockify 进程（跑稳，保持） |
| 窗口管理 | openbox（轻） | xfce4（完整桌面） |
| 密码 | vncpasswd DES 混淆 | passwdfile 明文 |
| 网页登录层 | ✅ WEB_AUTHENTICATION | ❌ 无 |
| HTTPS/VNC 加密 | ✅ SECURE_CONNECTION | ❌ 无 |
| CDP 暴露 | socat 桥隔离 | chromium 直监听 9222 |
| 缩放 | 三档含 Remote Scaling | 固定 local scale |

### 可借鉴点

1. **`WEB_AUTHENTICATION` 网页登录层** ⭐⭐⭐ —— 补公网 noVNC 无鉴权短板
2. **Remote Scaling 远端缩放** ⭐⭐⭐ —— 自动改 Xvnc 分辨率适配窗口
3. **Xvnc 一体架构** ⭐⭐ —— 单窗口应用场景更省（后来 v5 采纳！）
4. **socat CDP 桥 + 证书/HTTPS** ⭐

> 不采纳：nginx websockify 模块（自带 Python websockify 已跑稳，不为省一个进程换）。

---

## v5 Xvnc 动态分辨率 + 控制栏收起

> 2026-08-14 · chromium-QwenPaw v5
> 核心：把 v4 学到的 jlesage 架构真正落地——用 TigerVNC 的 Xvnc 替换"Xvfb + x11vnc"两件套，一次解决两个老痛点：分辨率锁死 + noVNC 侧边控制栏默认展开。

### 根因分析

| 痛点 | 根因 |
|---|---|
| 分辨率锁死 720x1280 | Xvfb 的 framebuffer 创建时固定，`xrandr -s` 报 Size not found |
| 侧边栏一直显示 | 旧入口跳 vnc_auto.html（精简 UI）+ patch 过样式 |

### 关键验证：Xvfb vs Xvnc

```bash
# Xvfb (失败 - framebuffer 锁死)
Xvfb :1 -screen 0 720x1280x24
$ DISPLAY=:1 xrandr -s 1280x720
Size 1280x720 not found in available modes   # ❌

# Xvnc TigerVNC (成功 - 自带 RANDR 动态分辨率)
Xvnc :1 -geometry 720x1280 -depth 24
$ DISPLAY=:1 xrandr -s 1280x720
# ✅ current 1280x720, 返回码 0, chromium 窗口自动跟随
```

**结论**：jlesage 能切分辨率是因为底层用了 **Xvnc（TigerVNC）**，它的 RANDR 实现支持运行时改 framebuffer 尺寸。这是整个方案的灵魂。

### 改造方案

```
旧架构 (v4): Xvfb :1 (锁死) → x11vnc :5900 → websockify :8080 → vnc_auto.html (精简, 侧边栏展开)
新架构 (v5): Xvnc :1 (动态) ─5900─ websockify :8080 → vnc.html (完整 UI, 控制栏默认收起)
   └─ xrandr -s 1280x720 (任意时刻切横屏)
```

supervisor 关键参数：

```ini
[program:xvfb]
command=/bin/sh -c "rm -f /tmp/.X1-lock ...; exec /usr/bin/Xvnc :1 -geometry 720x1280 -depth 24 -SecurityTypes None -localhost -AcceptSetDesktopSize=1 -AlwaysShared -rfbport 5900"
```

### 关键坑

1. **supervisor 改了配置必须 `reread` + `update`**，直接 start 还是旧命令（踩过，进程显示 /usr/bin/Xvfb 一脸懵）。
2. **Xvnc 1.12 语法是 `-geometry WxH` 不是 `-screen 0 WxHxD`**（Xvfb 语法）。
3. **控制栏默认收起是 noVNC 自带行为**（base.css `left: -100%`），用 vnc.html 完整 UI 就有，不用 patch。

### 验证

- 手机 720x1280：控制栏收起，只有左侧 ~15px 蓝色手柄 ✅
- 电脑 1280x720：`vnc-resize.sh desktop` 一键切横屏，chromium 自适应 ✅
- 公网 frp 30208 访问正常 ✅

## v6 白屏排查与孤儿进程

> 2026-08-15 记录：VNC 白屏排查全过程 + 根因（supervisor stop 杀不干净 → 孤儿进程抢桌面）+ 修复与模板加固。

## 现象

用户打开 noVNC `http://IP:PORT/vnc.html` 看到**白屏**，以为 chromium 有头模式没开。

## 排查过程

1. **supervisor 状态全绿**：chromium-cdp / chromium-gui / vnc-browser / xvfb / xfce4 全部 RUNNING，CDP 9222 正常，公网 noVNC 200 —— 服务层没问题。
2. **`DISPLAY=:1 xdotool search` 列出所有可见窗口**，发现桌面上**两个 chromium 窗口叠在一起**：
   - `Tampermonkey - Chrome Web Store - Chromium`（719x1279）✅ 有头浏览器，正确
   - `about:blank - Chromium`（720x1280，全屏）⚠️ **白屏元凶**
3. **按窗口找 PID**：`xdotool getwindowpid` → about:blank 窗口属于 **chromium-gui**（PID 28450），Tampermonkey 属于 **chromium-cdp**（PID 26409）。
4. **结论**：chromium 有头模式一直开着（chromium-cdp），但 chromium-gui 的 `about:blank` + `--kiosk` 全屏窗口**盖在上面**，用户看到的就是白屏。

## 根因

**supervisorctl stop 杀不干净 chromium-gui**。

`chromium-gui.sh` 原来是**后台启动 chromium**（`/usr/bin/chromium ... &` + `CHROME_PID=$!` + while 守护循环），supervisor 只杀了脚本 shell（`/bin/sh`），**真正的 chromium 进程变成孤儿进程继续活着**，它的 `about:blank --kiosk` 全屏窗口一直占着桌面。

配置层面 chromium-gui 的 `autostart=false` 只防止了"开机自启"，**挡不住已经活着的进程**。

## 修复

1. `supervisorctl stop chromium-gui`（先停 supervisor 托管）
2. **按进程特征精准清理孤儿**：`pkill -f "chromium-gui-profile"`（匹配它独有的 `--user-data-dir=...chromium-gui-profile` 参数）。⚠️ 注意：`pkill -f chromium-gui` 会把命令行里含同样字符串的 shell 自己匹配到（安全拦截 / 自杀），必须用更独特的特征。
3. 验证：`about:blank` 窗口消失，桌面只剩 Tampermonkey 有头窗口 ✅

## 模板加固（install.sh + 本机脚本同步）

1. **`exec` 前台直启**（治本）：`chromium-gui.sh` 从「后台 & + 守护循环」改为 `exec /usr/bin/chromium ...` 前台直启。这样 supervisor stop 时能**正确杀掉整个进程树**，不会留孤儿。
2. **`--kiosk` → `--start-fullscreen`**：kiosk 无边框无地址栏，观感上像"网页/白屏"；start-fullscreen 保留完整浏览器 UI（标签栏+地址栏），更符合"浏览器 GUI"预期。
3. **启动页 `about:blank` → `${CDP_START_URL}`（默认 Tampermonkey 商店）**：避免"一进去就是空白页"的白屏观感；有头模式（CDP_HEADED=1）下 chromium-gui 不启动，无头模式（CDP_HEADED=0）下启动直接看到真实网页。
4. **supervisor conf + template 双写确认**：chromium-gui `autostart=false`（有头模式占桌面的是 chromium-cdp，gui 不该自动起）；模板同步保证容器重建后不回滚。

## 关键经验

- **supervisor stop 只杀脚本壳，杀不死后台 `&` 启动的子进程** → 脚本必须 `exec` 前台直启，否则留孤儿进程。
- **桌面白屏不一定是有头模式没开**，先 `DISPLAY=:1 xdotool search --onlyvisible --name ".*"` 看有几个窗口叠着。
- **pkill 匹配要选独有特征**（如 `--user-data-dir=...profile`），否则匹配到自己/被安全拦截。
- **chromium-gui 的 `about:blank` + `--kiosk`** 是"白屏感"的两大来源：about:blank 是空白页，kiosk 隐藏所有浏览器 UI。
- 有头模式正确架构：**chromium-cdp（AI 自动化，人机同屏）占桌面**，chromium-gui 仅无头模式备用，autostart=false。

---

---

---

## v7 移动端适配与部署简化

> 2026-08-15 ~ 08-18 记录：iOS Safari 一系列适配修复 + Chrome 应用商店 UA 问题 + 去掉 FRP 隧道改本地直连 + noVNC 状态栏触摸优化。

### 7.1 iOS Safari 无法操作（resize=scale 双保险）

**现象**：iPhone（iOS 16.5 Safari）打开 noVNC 后点击/滚动/缩放都不行。

**根因**：
1. 本机 `vnc-browser.sh` 入口跳转是裸 `vnc.html`（无参数），noVNC 默认 `resize=off`：canvas 保持原始 720x1280，手机 390px 视口只显示左上角；加上 noVNC `base.css` 在 body 上设 `touch-action: none` → iOS 手势全被拦。
2. `ui.js` 默认值也仍是 `'off'`（直接访问 vnc.html 不带参数也落 off）。

**修复**：
- 入口改为 `vnc.html?autoconnect=1&resize=scale&show_dot=0`（客户端缩放铺满，桌面分辨率不变）
- 双保险：`ui.js` 默认 `initSetting('resize','off') → 'scale'`
- install.sh 模板加幂等 patch；commit `8ab8839`
- 实测 iOS 视口 390x844：连接成功、画面铺满、控制栏收起 ✅

### 7.2 iOS Safari 缩放不正常（viewport 锁定）

**现象**：安卓 UA 浏览器可以看，但 iOS Safari 显示缩放不正常。

**根因**：iOS Safari 会对不适合视口的页面做**页面级自动放大**（meta 是 `maximum-scale=3.0` 允许缩放），把 resize=scale 的 canvas 缩小到整页 → 比例错乱；安卓 Chrome 不做页面级自动放大故正常。

**修复**：`vnc.html` meta viewport 锁定：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no, viewport-fit=cover">
```

改完不用重启服务，强刷即可；写进 `novnc-chromesh-patch.py` 第 4 项（幂等）；commit `80f2b65`。

### 7.3 Chrome 应用商店打不开（UA 伪装惹的祸）

**现象**：谷歌商店打不开（之前能开），商店页跳到 `/unsupported`。

**根因**：为了让手机端看着像手机，之前给 chromium 挂了 iPhone Safari 的 **UA 伪装**；而 Google 现在只让“桌面浏览器”访问 Chrome 应用商店，手机 UA 一律 302 到 unsupported。

**修复**：移除 `--user-agent` iPhone 伪装 → 恢复原生桌面 UA；实测商店 200 正常。commit `093940f`。

> 教训：**商店一开始能打开，是因为装油猴时用的是桌面 UA**；后来加伪装才弄挂的。伪装 UA 会影响依赖桌面 UA 的站点，加之前先想清楚。

### 7.4 去掉 FRP 隧道：改为本地直连部署

**需求**：`install.sh` 里隧道部分可以去除——商店打不开跟隧道无关，纯本地部署更简洁。

**改动**（install.sh + README 重写，-238 行/+31 行）：
- 移除 frpc 自动下载/安装、frpc.toml 生成、SSH 隧道 + root 密码配置、FRP 参数（`-s/-t/-q/-p/-v/-S`）
- supervisor 不再托管 frpc，6 个服务：`xvfb xfce4 vnc-browser chromium-gui qwenpaw qwenpaw-backup`
- 新增 `-V` noVNC 端口 / `-Q` 面板端口 / `-P` 密码 / `-r` 分辨率
- 部署后访问：`http://localhost:8080/vnc.html` + `http://localhost:8088`
- commit `cbf5c2b` + `f10d67e`

> 注：本机公网 noVNC `http://165.1.122.72:30208` 走的是 frpc 手动托管（supervisor），不受 install.sh 改动影响，隧道照常跑。

### 7.5 状态栏触摸优化（底部不挡左右滑动）

**现象**：noVNC 底部“已连接”状态栏常显，手机左右滑动页面时被它挡住。

**修复**（base.css + ui.js）：
1. **瘦身**：状态栏从 ~36px 压到 18px（占屏 4%），字体 10px、单行省略、半透明背景
2. **穿模**：`#noVNC_status { pointer-events: none !important }` → 触摸直接穿透到桌面
3. **可点掉**：`hideStatus()` 改为彻底隐藏（visibility+opacity），下次连接/状态变化自动复现
4. 手机窄屏（≤440px）状态栏只占左侧 70% 宽，不档右侧滑动区

验证：本地 8080 + 公网 30208 均生效；390px 视口下状态栏仅剩底部 18px 细条，其余区域全可自由滑动 ✅

### 7.6 【遗留】画面单页适配（不需要左右滑动）

目标：noVNC 页面在手机上**一屏显示、无需左右滑动**。思路：
- 桌面 canvas 用 `resize=scale` 铺满视口宽（已默认）
- 消除页面级横向溢出：`html, body { overflow-x: hidden }` + 控制栏默认收起（不在手机屏上展开）
- 详见本文档 v7 结尾的落地记录（若有）

---

## 考古：chrome.sh 左侧菜单栏 & Sap-FireFox 固定画面

> 2026-08-14 · 应需求回查 Pterodactyl-Browser（chrome.sh / runfirefoxh.sh / ff_liteh.sh）

### 一、chrome.sh 的左侧菜单栏怎么来的

**它不是自定义的，就是 noVNC 的控制栏（Control Bar）**！chrome.sh 的核心操作：

```bash
cd novnc
[ -f "vnc.html" ] && [ ! -f "index.html" ] && mv vnc.html index.html   # ① 用完整 UI
enable_autoconnect index.html                                          # ② 85行插 autoconnect
sed -i 's/UI.initSetting("resize".*/UI.initSetting("resize","scale");/' index.html  # ③ 缩放=scale
```

- **vnc.html 是 noVNC 的完整 UI**（带控制栏/设置/剪贴板/键盘），控制栏锚点 `#noVNC_control_bar_anchor` **默认在左侧**（CSS 里没有 `noVNC_right` class 时就是左对齐）。
- **这就是"左侧菜单栏"**：一条蓝色竖条（手柄）+ 点击展开的工具栏（Settings / Clipboard / 键盘 / 全屏 / 断开）。
- 收起时只有边缘手柄，展开时才是完整菜单栏。**手机上看到"侧边菜单栏"= 控制栏处于展开状态**。

### 二、Sap-FireFox 怎么固定画面（手机 720x1280 固定）

runfirefoxh.sh（伪装进程名 mc-monitor/mc-box/mc-runtime）的固定画面三板斧：

```bash
# ① Xvnc 固定 720x1280 (不可动态切)
XVNC_ARGS=":1 -geometry 720x1280 -rfbport ${VNC_PORT} -SecurityTypes None"

# ② Firefox 窗口固定等于屏幕
$MC_RUNTIME_NAME --no-remote --no-sandbox --width=720 --height=1280 &

# ③ openbox 轻量 WM (无任务栏/无边框, 菜单走 menuh.xml)
curl -LSs https://gbjs.serv00.net/tar/menuh.xml -o ~/.config/openbox/menu.xml

# ④ noVNC: vnc.html → index.html + autoconnect
[ -f "vnc.html" ] && [ ! -f "index.html" ] && mv vnc.html index.html
enable_autoconnect "index.html"
```

**"固定画面"的本质**：
1. **Xvnc 固定 720x1280** —— 服务器端 framebuffer 固定，客户端怎么缩放都改变不了实际分辨率（只有 noVNC 的 resize=scale 做客户端缩放铺满）。
2. **浏览器窗口 = 屏幕尺寸** —— `--width=720 --height=1280`，窗口管理器（openbox）不画边框/任务栏，浏览器内容就是全屏。
3. **noVNC resize=scale** —— 网页端把 720x1280 画面按浏览器窗口等比缩放（手机屏幕小就缩小显示，但比例固定竖屏）。

### 三、对我们方案的启发

| 参考 | 可借鉴 | 我们的现状 |
|---|---|---|
| chrome.sh 左侧菜单栏 | 就是 noVNC 控制栏，**默认收起**（v5 已实现） | ✅ vnc.html + 控制栏收起 |
| Sap-FireFox 固定画面 | 浏览器窗口=屏幕 + 无边框 WM | ✅ chromium --kiosk/--window-size 铺满 |
| Xvnc 固定 vs 动态 | Sap 用固定（简单）；我们 v5 升级为**动态**（更灵活） | ✅ Xvnc RandR 动态 |
| openbox 轻量 WM | 单窗口应用更省 | 我们跑 xfce4 完整桌面（更重但功能全） |

**结论**：我们 v5 架构已经是"集两家之长"——既保留了 noVNC 完整 UI（控制栏默认收起，比 Sap 的固定画面更清爽），又把 Xvnc 从"固定"升级为"动态分辨率"（手机/电脑随意切，比两个参考项目都灵活）。

---

---

---

## 架构演进总览

```
v1   Xvfb + x11vnc + websockify + 两个浏览器(无头CDP + GUI)
v2   + chromium-cdp 有头化 = 人机同屏
v3   + 一端口三服务 + 统一密码
v4   + 学习 jlesage (Xvnc+nginx) 方案, 记 4 个可借鉴点
v5   + Xvfb→Xvnc 动态分辨率 + vnc.html 控制栏收起 + vnc-resize.sh
```

**最终架构（v5）**：

```
手机/电脑 ──▶ frp 30208 ──▶ websockify :8080 ──▶ Xvnc :1 (720x1280 动态) ──▶ chromium-cdp 有头 (CDP 9222)
                                    │                                            │
                                    └─ vnc.html (完整 UI, 控制栏默认收起)           └─ AI 自动化 + 用户同屏
```

**文件清单**：
- `/mnt/envd/vnc-browser/vnc-browser.sh`、`vnc-resize.sh`、`chromium-gui.sh`
- 仓库 `scripts/vnc-browser-xvnc.sh`、`scripts/vnc-resize.sh`
- supervisor：`[program:xvfb]` = Xvnc、`[program:chromium-cdp]` 有头、模板已同步

**下一步可做**：
- [ ] noVNC 前端加分辨率切换按钮（读 xrandr 模式列表，点击即发 SetDesktopSize）
- [ ] 开机自动检测：手机 UA 跳竖屏 / 电脑 UA 跳横屏
- [ ] `WEB_AUTHENTICATION` 网页登录层（v4 记的账）
- [ ] 把 vnc-resize.sh 接入 frp 隧道外网远程切换
